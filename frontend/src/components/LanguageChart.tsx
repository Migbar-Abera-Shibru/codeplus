import type { LanguageStats } from '../services/api';
import { BarChart, Bar, XAxis, YAxis, Tooltip, ResponsiveContainer, Cell } from 'recharts';
const COLORS=['#c084fc','#67e8f9','#f0abfc','#a5b4fc','#5eead4','#e4e4e7'];
export function LanguageChart({languages}:{languages:LanguageStats[]}) { const data=(languages||[]).slice(0,6).map(x=>({name:x.language,value:Number(x.percentage)||0})); return <section className="surface min-h-[340px] rounded-3xl p-6"><div className="mb-6"><p className="font-mono-label text-[10px] text-fuchsia-300">Language mix</p><h3 className="mt-2 text-lg font-semibold text-zinc-100">What they build with</h3></div>{data.length?<div className="h-[245px]"><ResponsiveContainer width="100%" height="100%"><BarChart data={data} layout="vertical" margin={{left:8,right:16}}><XAxis type="number" hide/><YAxis type="category" dataKey="name" width={80} tick={{fill:'#a1a1aa',fontSize:11}} axisLine={false} tickLine={false}/><Tooltip cursor={{fill:'rgba(255,255,255,.04)'}} contentStyle={{background:'#18181b',border:'1px solid #3f3f46',borderRadius:12,color:'#fff'}} formatter={(value)=>[`${Number(value).toFixed(1)}%`,'Share']}/><Bar dataKey="value" radius={[0,5,5,0]} barSize={18}>{data.map((_,i)=><Cell key={i} fill={COLORS[i%COLORS.length]}/>)}</Bar></BarChart></ResponsiveContainer></div>:<div className="grid min-h-[240px] place-items-center text-sm text-zinc-500">No language data available.</div>}</section>; }
