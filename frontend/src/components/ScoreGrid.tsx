import type { DeveloperReport } from '../services/api';
const scores=[['Consistency','Commit regularity','consistency_score'],['Complexity','Project depth','complexity_score'],['Collaboration','Community engagement','collaboration_score'],['Overall','Combined signal','overall_score']] as const;
export function ScoreGrid({data}:{data:DeveloperReport}) { return <div className="grid grid-cols-2 gap-3 lg:grid-cols-4">{scores.map(([label,copy,key])=>{const value=Math.max(0,Math.min(100,Number(data[key])||0));return <div key={label} className={`surface rounded-2xl p-5 ${label==='Overall'?'border-fuchsia-300/25':''}`}><div className="flex items-start justify-between gap-3"><div><p className="font-mono-label text-[10px] text-zinc-500">{label}</p><p className="mt-2 text-xs text-zinc-600">{copy}</p></div><span className="font-mono text-2xl font-bold text-zinc-100">{value}</span></div><div className="mt-5 h-1.5 rounded-full bg-zinc-800"><div className="h-full rounded-full bg-gradient-to-r from-fuchsia-300 to-cyan-300" style={{width:`${value}%`}}/></div></div>})}</div>; }
