import { AlertTriangle, ArrowLeft, RefreshCw } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
export function ErrorDisplay({error}:{error:any}){const navigate=useNavigate();const status=error?.response?.status??error?.status;const copy=status===404?['Profile not found','Check the username and make sure the profile is public.']:status===429?['Too many requests','GitHub is asking us to slow down. Try again in a moment.']:status===504?['Analysis timed out','The profile is large or GitHub is busy. Please retry.']:['Something interrupted the analysis','We could not finish reading this profile. Try again.'];return <div role="alert" className="surface rounded-2xl p-6"><div className="flex gap-4"><div className="grid size-10 shrink-0 place-items-center rounded-xl bg-amber-300/10 text-amber-200"><AlertTriangle className="size-5"/></div><div><h2 className="font-semibold text-zinc-100">{copy[0]}</h2><p className="mt-1 text-sm leading-6 text-zinc-500">{copy[1]}</p><div className="mt-5 flex flex-wrap gap-3"><button onClick={()=>window.location.reload()} className="focus-ring inline-flex items-center gap-2 rounded-full bg-zinc-100 px-4 py-2 text-sm font-semibold text-zinc-950"><RefreshCw className="size-4"/> Try again</button><button onClick={()=>navigate('/')} className="focus-ring inline-flex items-center gap-2 rounded-full border border-white/10 px-4 py-2 text-sm text-zinc-300"><ArrowLeft className="size-4"/> Back home</button></div></div></div></div>; }
