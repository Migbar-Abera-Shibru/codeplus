import { motion } from 'framer-motion';
import { Star, GitFork, ExternalLink, Code2, ChevronRight } from 'lucide-react';
import type { RepositoryComplexity } from '../services/api';
export function RepositoryList({ repos }: { repos: RepositoryComplexity[] }) {
  if (!repos?.length) return <section className="report-card empty-card"><Code2 /><p>No repository data available</p></section>;
  return <section className="report-card repos-panel"><div className="panel-heading"><div><p className="report-kicker">PROJECT SIGNAL</p><h2>Top Projects</h2><small>By complexity score</small></div><button className="ghost-action">View All Repositories <ChevronRight /></button></div><div className="repo-list">{repos.slice(0, 6).map((repo, index) => <motion.a key={repo.name} href={repo.url} target="_blank" rel="noreferrer" initial={{ opacity: 0, x: -8 }} animate={{ opacity: 1, x: 0 }} transition={{ delay: index * .04 }} className="repo-row"><div className="repo-mark"><Code2 /></div><div className="repo-name"><strong>{repo.name}</strong><span>{repo.language || 'Code'}</span></div><div className="repo-meta"><span><Star /> {repo.stars}</span><span><GitFork /> {repo.forks}</span></div><b className="repo-score">Score: {repo.score}</b><em>{repo.complexity_tier}</em><ExternalLink className="repo-external" /></motion.a>)}</div>{repos.length > 6 && <p className="repo-more">+{repos.length - 6} more repositories</p>}</section>;
}
